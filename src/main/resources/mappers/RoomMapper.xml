<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.brickart.hotelmanagement.persistence.mybatis.dao.RoomDao">

    <resultMap id="RoomResultMap" type="Room">
        <id property="id" column="id"/>
        <result property="hotelId" column="hotel_id"/>
        <result property="roomNumber" column="room_number"/>
        <result property="floor" column="floor"/>
        <result property="available" column="is_available"/>

        <association property="roomType" javaType="RoomType">
            <id property="id" column="room_type_id"/>
            <result property="name" column="room_type_name"/>
            <result property="capacity" column="capacity"/>
            <result property="pricePerNight" column="price_per_night"/>
        </association>
    </resultMap>

    <insert id="create" parameterType="Room" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO rooms (hotel_id, room_type_id, room_number, floor, is_available)
        VALUES (#{hotelId}, #{roomType.id}, #{roomNumber}, #{floor}, #{available})
    </insert>

    <update id="update" parameterType="Room">
        UPDATE rooms
        SET hotel_id = #{hotelId},
        room_type_id = #{roomType.id},
        room_number = #{roomNumber},
        floor = #{floor},
        is_available = #{available}
        WHERE id = #{id}
    </update>

    <delete id="delete" parameterType="long">
        DELETE FROM rooms WHERE id = #{id}
    </delete>

    <select id="findById" parameterType="long" resultMap="RoomResultMap">
        SELECT r.id, r.hotel_id, r.room_number, r.floor, r.is_available,
        rt.id AS room_type_id,
        rt.name AS room_type_name,
        rt.capacity,
        rt.price_per_night
        FROM rooms r
        JOIN room_types rt ON rt.id = r.room_type_id
        WHERE r.id = #{id}
    </select>

    <select id="findByHotelId" parameterType="long" resultMap="RoomResultMap">
        SELECT r.id, r.hotel_id, r.room_number, r.floor, r.is_available,
        rt.id AS room_type_id,
        rt.name AS room_type_name,
        rt.capacity,
        rt.price_per_night
        FROM rooms r
        JOIN room_types rt ON rt.id = r.room_type_id
        WHERE r.hotel_id = #{hotelId}
    </select>

</mapper>